spring:
  application:
    name: libraryapp
  jpa:
    database: postgresql
    show-sql: false
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:5432/postgres
    username: postgres.bnyjruudrrbglhapbyzg
    password: oaAmkIgF7o2K9EFn

springdoc:
  swagger-ui:
    # Change the default Swagger UI path
    path: /swagger-ui.html
  api-docs:
    # Change the default JSON contract path
    path: /v1/api-docs